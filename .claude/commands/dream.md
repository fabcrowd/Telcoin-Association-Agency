# /dream

Run a dreaming pass — consolidate recent session output files into the agency memory store.

The dreaming pass also runs nightly as a scheduled Routine. Use `/dream` to trigger it manually
between scheduled runs, or after a heavy session where you want the memory files updated immediately.

## Instructions

Read and execute `scripts/dreaming-pass.md` in full, exactly as written. No modifications.
